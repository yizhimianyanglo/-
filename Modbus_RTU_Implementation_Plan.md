# Modbus RTU 13 项采集与控制队列

## 总体目标

在 Qt 5.15 下使用 `QModbusRtuSerialMaster` 与 Modbus Slave 模拟器通信，实现：

- 13 项监测数据每秒轮询；
- 健康响应完成后 200ms 内刷新界面；
- 读请求 FIFO；
- 写请求队首插队；
- 请求去重和最新值替换；
- 请求级 3 秒超时、首次发送加 3 次重试；
- 1s/2s/4s/30s 重连退避；
- Online、Unstable、Error、Offline 四态设备模型；
- 控制页写入结果反馈；
- 失联后保留最后有效值并标记陈旧。

## 点表与配置

增加外部 `modbus.ini`，默认通信参数：

```ini
port=COM1
baudRate=9600
parity=Even
dataBits=8
stopBits=1
pollIntervalMs=1000
requestTimeoutMs=3000
maxRetries=3
storageIntervalMs=5000
```

模拟器演示点表固定为从站 1：

- 保持寄存器 `0..12`：13 项传感器数据。
- 线圈 `0..5`：投喂、温度、水位、环境、增氧、太阳能自动开关。
- 保持寄存器 `100..106`：投喂频次、投喂量、水流速、电源模式、通信模式、增氧时长、增氧次数。

13 项数据保持现有顺序：

`Temp`、`Humidity`、`Pressure`、`VOC`、`Light`、`PH`、`TDS`、`EC`、`Turbidity`、`Temp1`、`Temp2`、`WaterLevel1`、`WaterLevel2`。

演示倍率由配置提供，例如温度使用 `/10`、PH 使用 `/100`、EC 使用 `/100`。所有地址、倍率和数据类型均可在现场点表确认后只修改 INI，不改通信代码。地址统一使用 Modbus PDU 零基地址。

## 通信模块

重构现有 `modbusmanger.*`：

- 类名修正为 `ModbusManager`，底层使用 Qt 5.15 的 `QModbusRtuSerialMaster`。
- 通信对象移动到独立 `QThread`，串口、定时器和请求处理全部在通信线程执行。
- 保留 `QQueue<ModbusRequest>`：
  - 读请求使用 FIFO；
  - 写请求使用 `prepend()` 插入队首；
  - 当前请求不可被打断，完成后优先处理写请求。
- 请求唯一键包含请求类型、从站、寄存器区、起始地址和数量：
  - 相同读请求只保留一份；
  - 相同地址相同值的写请求复用已有请求；
  - 相同地址不同值时删除尚未发送的旧值，保留最新值并放到队首；
  - 当前正在发送的旧写请求不能取消，完成后发送最新值。
- Qt 客户端超时设置为 3 秒，关闭 Qt 内置重试，统一由请求对象管理首次发送和最多 3 次重试，避免双重重试。
- 请求完成后统一清理当前 reply、更新统计、发出成功或失败信号，确保超时、发送失败和断线不会重复释放或重复推进队列。
- 串口断开时：
  - 当前和排队中的写请求全部失败并通知界面，要求操作员重新下发；
  - 清理旧读请求；
  - 重连成功后重新建立周期读请求。
- 重连失败按 `1s -> 2s -> 4s -> 30s -> 30s...` 退避；成功后停止重连定时器并恢复轮询。

## 设备状态与数据接口

每个从站维护成功次数、失败次数、连续超时次数、最后成功时间和当前状态：

- `Online`：连续超时 0~2 次，按 1 秒周期读取；
- `Unstable`：连续超时 3~4 次，按 3 秒周期探测；
- `Error`：连续超时 5~9 次，按 10 秒周期探测；
- `Offline`：连续超时达到 10 次，按 30 秒周期探测；
- 任意一次成功立即恢复 `Online` 并清零连续超时计数。

新增按值传输的 `MonitorSnapshot`，包含：

- 13 项数据值；
- 每项数据质量 `Good/Stale/Bad`；
- 当前采集时间；
- 从站和请求统计信息。

数据链路信号包括：

- `snapshotReady(const MonitorSnapshot&)`
- `connectionStateChanged(...)`
- `slaveStateChanged(int slaveId, DeviceState state, ...)`
- `writeRequestFinished(quint64 requestId, bool success, ...)`
- `requestFailed(quint64 requestId, ...)`

健康情况下，一次完整 Modbus 响应完成后立即生成快照并刷新界面；验收延迟从 `QModbusReply::finished` 到界面更新开始计时，目标小于 200ms。发生超时重试时，允许该轮采集超过 1 秒，不把故障场景与健康刷新指标混淆。

## 界面、控制和存储

- `MainWindow`、`WaterTop`、`WaterBottom` 和 `GraphData` 改用 `MonitorSnapshot`，移除裸 `double*`。
- 正常值实时刷新；失联字段保留最后有效值并显示陈旧状态，首次没有有效值时显示 `--`。
- 陈旧或无效值不参与越限报警。
- 控制页 6 个自动开关和 7 个参数项统一调用高优先级写接口：
  - 写响应正常后才更新界面状态；
  - 写失败保留原状态并记录失败原因；
  - 断线后的写请求不自动补发。
- 趋势图维护最近 10 次实时有效采样，不再每秒查询数据库。
- 每 5 秒将最新快照写入 `MonitorData`；陈旧字段写入 `NULL`。
- MySQL 暂时不可用时继续采集和显示，每 30 秒重连；不缓存离线期间数据。
- 从 qmake 构建中移除旧 TCP 数据源和旧数据库状态线程，保留源文件但不再被主程序使用。

## 测试与验收

增加 QtTest 覆盖：

- 13 项寄存器解码、倍率换算和非法配置；
- 读 FIFO 顺序；
- 写请求队首插队；
- 相同请求去重；
- 不同值写请求替换旧值；
- 首次发送加 3 次重试；
- `Online -> Unstable -> Error -> Offline` 状态转换；
- `1/2/4/30` 秒重连时间序列；
- 断线清理写请求；
- 快照质量和陈旧值处理。

使用 Modbus Slave 模拟器及虚拟串口对验证：

1. 从站 1 以 `9600/8E1` 响应 `0..12` 寄存器。
2. 监测界面每秒更新 13 项数据。
3. 响应完成到界面刷新小于 200ms。
4. 连续写入同一控制地址时只执行最新值。
5. 模拟超时、断线、恢复连接，确认状态转换、降频探测和自动续接。
6. 断开数据库后确认 Modbus 采集仍持续，数据库恢复后新数据继续写入。

实际设备上线前，只需将演示点表替换为现场传感器和控制器点表；通信状态机、队列语义和界面接口保持不变。
